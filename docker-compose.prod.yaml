version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: BjjTrack-prod
    environment:
      POSTGRES_DB: ${POSTGRES_DB_PROD}
      POSTGRES_USER: ${POSTGRES_USER_PROD}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD_PROD}
    ports:
      - "5431:5432"
    restart: always
